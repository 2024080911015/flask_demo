# 使用社交网络数据改进推荐算法

## 问题分析

当前系统只使用了以下数据：
- `user_embeddings.pt` - 用户向量嵌入
- `users.csv` - 用户个人信息

但忽略了三个重要的社交网络数据文件：
- `edges.csv` - 学生关注关系（邻接表格式）
- `edges_ID.csv` - 关注行为时序记录
- `edges_time.csv` - 包含时间戳的关注历史

## 改进方案

### 方案一：在推荐中加入社交网络信息（简单版）

#### 修改 step3_recommend.py

```python
import torch
import torch.nn.functional as F
import pandas as pd
import os

print("🚀 启动时序图推荐推理引擎...")

# 1. 加载模型产出的嵌入向量
try:
    embeddings = torch.load('user_embeddings.pt', map_location='cpu', weights_only=False)
except FileNotFoundError:
    print("❌ 找不到 user_embeddings.pt")
    exit()

# 2. 安全加载用户信息
try:
    csv_path = os.path.join(os.path.dirname(os.path.abspath(__file__)), 'users.csv')
    df_users = pd.read_csv(csv_path, encoding='utf-8')
except UnicodeDecodeError:
    df_users = pd.read_csv(csv_path, encoding='gbk')

user_info_map = pd.Series(df_users['info'].values, index=df_users['uid']).to_dict()

# 3. 加载社交网络数据 (新增)
def load_social_data():
    """加载社交网络数据"""
    try:
        # 加载 edges.csv (关注关系)
        edges_path = os.path.join(os.path.dirname(os.path.abspath(__file__)), 'edges.csv')
        df_edges = pd.read_csv(edges_path)
        # 构建关注字典: {user_id: [following_ids]}
        follow_dict = {}
        for idx, row in df_edges.iterrows():
            user_id = row['source']
            # 将target列按|分割成列表
            following_ids = list(map(int, str(row['target']).split('|')))
            follow_dict[user_id] = following_ids

        print(f"✅ 加载了 {len(follow_dict)} 个用户的关注关系")

        return follow_dict

    except Exception as e:
        print(f"⚠️  加载社交网络数据失败: {e}")
        return {}

follow_dict = load_social_data()

# 4. 核心推荐算法 - 改进版
def recommend_friends(user_id, top_k=5):
    u_idx = user_id - 1
    if u_idx < 0 or u_idx >= embeddings.shape[0]:
        return []

    target_emb = embeddings[u_idx].unsqueeze(0)
    similarity = F.cosine_similarity(target_emb, embeddings)

    # 获取最高分的几个人
    scores, indices = torch.topk(similarity, k=top_k + 10)
    recommend_ids = (indices + 1).tolist()

    if user_id in recommend_ids:
        recommend_ids.remove(user_id)

    # 加入社交网络信息优化
    # 方式1: 优先推荐用户已关注的人的相似用户
    final_rec = []
    user_following = follow_dict.get(user_id, [])

    # 首先从推荐列表中筛选出与用户关注的人相似的用户
    for rid in recommend_ids:
        if rid in user_following:
            continue  # 不推荐已关注的人

        # 检查推荐用户是否与目标用户有共同关注
        rid_following = follow_dict.get(rid, [])
        common_following = set(user_following) & set(rid_following)

        if len(common_following) > 0:
            final_rec.append(rid)
            if len(final_rec) >= top_k:
                return final_rec

    # 如果不够，从剩余的推荐列表中补充
    remaining = [rid for rid in recommend_ids if rid not in final_rec and rid not in user_following]
    final_rec.extend(remaining)

    return final_rec[:top_k]

# 4. 测试
if __name__ == "__main__":
    while True:
        try:
            val = input("\n👉 请输入学生ID (1-1000) (输入 q 退出): ")
            if val.lower() == 'q': break
            uid = int(val)

            print(f"\n🔍 [学生 {uid}] 的档案: {user_info_map.get(uid, '未知')}")
            print(f"📱 关注: {follow_dict.get(uid, [])}")
            recs = recommend_friends(uid)
            print("✨ 依据最新时间演化图，为您推荐：")
            for rid in recs:
                print(f"   ➤ ID: {rid:03d} | {user_info_map.get(rid, '未知')}")
        except Exception as e:
            print(f"❌ 错误: {e}")
```

### 方案二：新增社交网络分析接口

#### 在 app.py 中新增接口

```python
# 新增：获取用户关注列表
@app.route('/following')
def get_following():
    sid = request.args.get('id', default=None, type=int)
    if sid is None:
        return jsonify({"error": "Missing 'id' parameter"}), 400

    following_list = follow_dict.get(sid, [])
    following_info_list = []
    for fid in following_list:
        following_info = user_info_map.get(fid, f"User {fid}")
        following_info_list.append(following_info)

    return jsonify({
            "student_id": sid,
            "following_count": len(following_list),
            "following": following_info_list
        })

# 新增：获取用户粉丝列表
@app.route('/followers')
def get_followers():
    sid = request.args.get('id', default=None, type=int)
    if sid is None:
        return jsonify({"error": "Missing 'id' parameter"}), 400

    # 从 follow_dict 反向查找粉丝
    followers_list = []
    for user_id, following in follow_dict.items():
        if sid in following:
            followers_list.append(user_id)

    followers_info_list = []
    for fid in followers_list:
        follower_info = user_info_map.get(fid, f"User {fid}")
        followers_info_list.append(follower_info)

    return jsonify({
            "student_id": sid,
            "followers_count": len(followers_list),
            "followers": followers_info_list
        })

# 新增：获取社交网络统计信息
@app.route('/social/stats')
def get_social_stats():
    total_users = len(user_info_map)
    total_follows = sum(len(following) for following in follow_dict.values())
    avg_follows = total_follows / total_users if total_users > 0 else 0

    # 计算关注最多的用户
    max_follows = 0
    most_popular = []
    for user_id, following in follow_dict.items():
        if len(following) > max_follows:
            max_follows = len(following)
            most_popular = [user_id]
        elif len(following) == max_follows:
            most_popular.append(user_id)

    return jsonify({
        "total_users": total_users,
        "total_follows": total_follows,
        "average_follows": round(avg_follows, 2),
        "max_follows": max_follows,
        "most_popular_users": most_popular
    })
```

## 实施步骤

### 步骤1：备份原始代码

```bash
cp step3_recommend.py step3_recommend.py.old
cp app.py app.py.old
```

### 步骤2：修改 step3_recommend.py

- 添加 `load_social_data()` 函数
- 改进 `recommend_friends()` 函数
- 在测试部分添加关注信息显示

### 步骤3：修改 app.py

- 从 step3_recommend.py 导入 follow_dict
- 新增 `/following` 接口
- 新增 `/followers` 接口
- 新增 `/social/stats` 接口

### 步骤4：测试新功能

```bash
python app.py
```

测试以下接口：
- `/tuijian?id=1` - 查看推荐结果
- `/following?id=1` - 查看ID=1的关注列表
- `/followers?id=1` - 查看ID=1的粉丝
- `/social/stats` - 查看社交网络统计

## 优化建议

### 1. 使用 edges_time.csv 进行时序分析

如果需要更精细的时序分析，可以加载 `edges_time.csv`：

```python
def load_follow_timeline():
    """加载关注时间序列数据"""
    try:
        df_time = pd.read_csv('edges_time.csv')
        timeline_dict = {}  # {user_id: [(follow_id, timestamp), ...]}
        for idx, row in df_time.iterrows():
            user_id = row['Source_ID']
            records = str(row['Following_Records_Chronological...']).split(',')
            follows = []
            for record in records:
                # 解析格式：关注:225(2024-09-01 07:44:23)
                if '关注:' in record:
                    try:
                        parts = record.split('(')
                        fid = int(parts[0].replace('关注:', ''))
                        ts = parts[1].replace(')', '')
                        follows.append((fid, ts))
                    except:
                        continue
            timeline_dict[user_id] = follows
        return timeline_dict
    except:
        return {}
```

### 2. 使用 edges_ID.csv 进行实时分析

`edges_ID.csv` 已经是标准的时序格式，加载更简单：

```python
def load_edge_timeline():
    """加载关注关系的时序记录"""
    try:
        df_edge_time = pd.read_csv('edges_ID.csv')
        # 可以按时间分组分析
        df_edge_time['timestamp'] = pd.to_datetime(df_edge_time['timestamp'])
        return df_edge_time
    except:
        return pd.DataFrame()
```

## 性能优化建议

1. **数据缓存**：首次加载后将数据缓存
2. **索引优化**：为用户ID字段创建索引
3. **批量处理**：批量处理推荐请求
4. **异步加载**：使用线程或协程加载数据

## 总结

本次修改新增了以下功能：

✅ **推荐算法优化**：结合用户关注关系
✅ **社交网络分析**：新增3个社交网络接口
✅ **数据完整性**：使用了之前未使用的edges.csv
✅ **用户体验**：推荐结果更符合社交规律

这些改进可以显著提升推荐系统的质量，让推荐结果更符合用户的社交网络特征。
